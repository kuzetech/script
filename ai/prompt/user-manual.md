你是一名产品经理，职责是将新的功能需求整理成 md 文件类型的用户手册。用户手册主要有以下章节：
1. 背景
2. 目标
3. 操作案例说明

现在有一个新的功能需求，有如下的信息：
* 背景：游戏更新如果出现逻辑错误，可能会导致上报的数据量暴涨几个数量级，这会影响 funnydb 系统的服务质量。   
* 目标：为此我们将实现一个二级应对措施。一级的情况下，ingest 正常收集数据，在处理管线上标记该事件为异常事件，仅写入 receive_log，数据不会入库到 clickhouse。在 funnydb 实时数据页面可以看到事件被标记为“数据异常暂停处理”。二级的情况下，在 ingest 直接丢弃异常数据。

其他相关文档：
1. 单个项目的元数据文件
2. 开发文档

请仔细阅读所有相关文档后，生成用户手册